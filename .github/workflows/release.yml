name: Build and Release Skills

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., v1.0.0)'
        required: true
        type: string
      release_notes:
        description: 'Release notes (optional)'
        required: false
        type: string
        default: ''

permissions:
  contents: write

jobs:
  test-and-release:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install docx package
        run: npm install docx
      
      - name: Run penitential-act-formatter tests
        run: node penitential-act-formatter/scripts/test_generate_penitential_act.js
      
      # Add more test steps here as you add skills:
      # - name: Run another-skill tests
      #   run: node another-skill/scripts/test_another_skill.js
      
      - name: Package penitential-act-formatter skill
        run: |
          cd penitential-act-formatter
          zip -r ../penitential-act-formatter.skill SKILL.md scripts/
      
      - name: Package penitential-act-invocations skill
        run: |
          cd penitential-act-invocations
          zip -r ../penitential-act-invocations.skill SKILL.md

      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ inputs.version }}
          name: ${{ inputs.version }}
          body: |
            ## Catholic Liturgy Skills ${{ inputs.version }}
            
            ${{ inputs.release_notes }}
            
            ### Included Skills
            - **penitential-act-formatter.skill** - Generates formatted Penitential Act documents
            - **penitential-act-invocations.skill** - Invocation sequences for the Penitential Act skill
            
            ### Installation
            1. Download the `.skill` file for the skill you want
            2. Upload it to Claude
          files: |
            penitential-act-formatter.skill
            penitential-act-invocations.skill
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
